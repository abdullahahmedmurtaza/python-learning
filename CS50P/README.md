Run the tests using Python 3.12 as submit50 and check50 break on Python 3.13.

Run 'pip install -r requirements.txt -y' to install the necessary libraries, if some are missing, they can be installed on the go.